package ${warpper.packageName};

<% for(import in warpper.imports!){ %>
import ${import};
<% } %>

/**
 * ${context.domainBigName} 包装类
 *
 * @author liangchangchun
 * @Date ${tool.currentTime()}
 */
public class ${context.bizEnBigName}Warpper extends BaseControllerWarpper {



	public ${context.bizEnBigName}Warpper(Object obj, Class type) {
		super(obj, type);
	}

	@Override
	protected void warpTheMap(Map<String, Object> map) {
		
	}

}
